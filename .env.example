# Example environment values for local development
# Do not commit real secrets.

NODE_ENV=development

# Optional renderer overrides
VITE_DEV_SERVER_URL=http://127.0.0.1:5317

# Optional backend defaults (the app mainly stores these in electron-store)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=orangehrm_user
MYSQL_PASSWORD=change_me
MYSQL_DATABASE=orangehrm
