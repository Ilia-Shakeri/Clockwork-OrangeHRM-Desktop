# Example environment values for local development
# Do not commit real secrets.

NODE_ENV=development

# Optional renderer overrides
VITE_DEV_SERVER_URL=http://127.0.0.1:5317

# Optional backend defaults (the app primarily stores active connection in electron-store)
# Supported values: mariadb | mysql | postgres | sqlite
DB_ENGINE=mariadb

# Used for mariadb/mysql/postgres
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=orangehrm_user
DB_PASSWORD=change_me
DB_NAME=orangehrm
DB_SSL=false

# Used for sqlite only
SQLITE_PATH=./data/app.db
